{{#if isLoading}}
    {{loading-view}}
{{/if}}
{{#if mms}}
    {{#each message.parts as |part|}}
        {{mms-part part=part}}
    {{/each}}
{{else}}
    <message-bubble>
        {{emoji-text content=message.body}}
    </message-bubble>
{{/if}}