{{#if isPlainText}}
    <message-bubble>
        {{emoji-text content=part.data}}
        {{#if sender}}
            <sender>
                {{sender}}
            </sender>
        {{/if}}
    </message-bubble>
{{else if isImage}}
    {{mms-image partId=part._id}}
    {{#if sender}}
        <sender>
            {{sender}}
        </sender>
    {{/if}}
{{/if}}