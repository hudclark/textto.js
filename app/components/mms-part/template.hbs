{{#if isPlainText}}
    <message-bubble>
        {{emoji-text content=part.data linkify=true}}
        {{#if sender}}
            <sender class="color-text-primary">
                {{sender}}
            </sender>
        {{/if}}
        {{#unless isScheduled}}
            {{link-preview text=part.data}}
        {{/unless}}
    </message-bubble>
{{else if isImage}}
    {{mms-image thumbnail=part.thumbnail url=part.fullImage encrypted=message.encrypted}}
    {{#if sender}}
        <sender>
            {{sender}}
        </sender>
    {{/if}}
{{/if}}