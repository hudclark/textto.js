<div class="settings-page">
    <div class="nav-bar">
        {{#link-to 'threads'}}<i class="material-icons">arrow_back</i>{{/link-to}}
        <span>Settings</span>
    </div>

    <div class="settings-container">

        <div class="settings-title">
            Notifications
        </div>

        {{setting-item
            type='switch'
            icon='notifications'
            displayName='Notifications'
            default=true
            setting='notifications'}}

        {{setting-item
            type='switch'
            icon='remove_red_eye'
            displayName='Hide NotificationText'
            default=false
            setting='hideNotificationText'}}

        {{setting-item
            type='number'
            displayName='Notification Length (seconds)'
            icon='access_time'
            default=3
            setting='notificationLength'}}

        {{setting-item
            type='switch'
            displayName='Mirror your phone\'s notifications'
            icon='devices'
            default=true
            setting='mirrorNotifications'}}

        <div class="settings-title">
            Encryption
        </div>

        {{encryption-setup}}

        {{#if model.upgradedSyncLimits}}
            <div class="settings-title">
                Sync older threads
            </div>
            <p>Since you've upgraded your sync limits, you can now retroactively sync more of your recent threads. Please note that only versions of Textto for Android v1.3.3 or above have this feature. It may take a few minutes to sync older conversations.</p>
            <div class="centered">
                {{#if syncingOldThreads}}
                    {{loading-view}}
                {{else}}
                    <a class="waves-effect cta small" {{action 'syncOldThreads'}}>Sync Threads</a>
                {{/if}}
            </div>
        {{/if}}

        <div class="settings-title">
            Other
        </div>

        <a class="waves-effect setting-item" href="https://textto.io/changes" target="_blank">
            <i class="material-icons">flash_on</i>
            Change Log
        </a>

    </div>
</div>